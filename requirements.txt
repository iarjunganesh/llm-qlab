# IMPORTANT: llama-cpp-python must be built from source for CUDA 13.2 / sm_120 (RTX 5070 series).
# The PyPI wheel does not include sm_120 CUDA kernels.
# See README "Source Build" instructions — do NOT just `pip install llama-cpp-python`.
llama-cpp-python

# Hugging Face Hub — download GGUF models
huggingface_hub

# Data & visualization (compare_quants.py)
pandas
matplotlib

# Optional: interactive GPU monitor CLI (run standalone: nvitop)
nvitop
